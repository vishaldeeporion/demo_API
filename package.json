{
  "name": "demoapisproject",
  "version": "1.0.0",
  "description": "This Project is use for make Demo API so that the Interns can practice the fatch, axios and other concepts which are using ani API endpoint. In this project only CRUD operation API is available.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vishal Jaiswal",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
